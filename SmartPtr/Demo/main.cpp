#include </Users/yanmi/source/repos/SmartPtr/SmartPtr/SmartPtr/SingleList.h>

int main() 
{
	SingleList<int> list;

	list.pushFront(1);
	list.pushFront(2);
	list.pushFront(3);
	list.pushFront(4);
	list.pushFront(5);
	list.show();
	
}